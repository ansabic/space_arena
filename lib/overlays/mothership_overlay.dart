import 'package:flutter/material.dart';

class MothershipOverlay extends StatelessWidget {
  const MothershipOverlay({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [],
    );
  }
}
